# Security

This library does not open a network connection, does not take an account, and does not store a secret.

Do not open a public issue that contains a private key, a token, or a file you did not mean to publish. Use the Security tab on this repository and send a private vulnerability report.

A refusal printed by the command is the library working. It is not a security boundary around someone else's data.
